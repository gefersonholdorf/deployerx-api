name: "Deploy DeployerX API Development"

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v3

      - name: Deploy
        run: echo "Deploy executando..."